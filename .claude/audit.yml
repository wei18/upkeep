# Upkeep auditing itself (dogfood).
# The docs/<locale>/plans/ tree is an archived design record (see design.md §6),
# kept verbatim from the zh-TW source — exclude it from the audit entirely.
ignore:
  - "docs/*/plans/**"
